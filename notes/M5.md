# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — Add a tags list to app.notes dict**: app.notes.anyelement has a title, body, and a list of tags. Every tag that a note has will be a unique string.  _Verify: existing tests still pass. A new test confrims that a newly created note has tags == []_
2. **Task 2 — Add Tags**: a new tag for a note exists in that note's tags list. _Verify: Adding a new item to a note's tags list adds a new element to the tags list._
3. **Task 3 — Pass tags list to html**: template render gets tags passed into it along with error, title, and body. Template iterates through tags list to display text for each existing tag. _Verify: existing tests still pass. A note with a tags list length greater than 0 results in all tags in the tags list being shown as a string below the note's title._
4. **Task 4 — All Tags List**: Add an app wide list of all existing tags. Then, create a helper function that is called whenever a tag is added to a note to check to see if that tag already exists in this all tags list. If it doesn't, add it. No duplicates allowed. _Verify: Add a new tag to a note and make sure that it shows up in app wide all tags list too._
5. **Task 5 — Filter Creation**: Add a section to the top of the webpage in the template called filters and then iterate through all tags to display a button for each one. _Verify: Add tags to all Tags app wide list, and then mske sure each of those tags are displayed as a benign button on the web page._
6. **Task 6 — Filter sort**: The buttons for each tag send a get request to server. The buttons pass the tag name as an extra parameter (q?="tag") and the user gets redirected to home _Verify: clicking on a tag button sends a get request to the server that's logged in the console with the tag's name sent through. User is redirected to home but the page contents don't change yet._
7. **Task 7 — Notes Sort**: rerender the page to only display notes with that tag. When a get request is recieved with the allowed tag, get method iterates through app.notes and only adds notes to a filteredNotes list if the note has the specified tag in it's tags list. That list is passed to the render template instead. _Verify: A list called filteredNotes is craeted and populated with every note that has the specified tag in the tags list. Page is rerendered only with those notes._

## Task 1 context brief
1. **Task 1 — Add a tags list to app.notes dict**: app.notes.anyelement has a title, body, and a list of tags. Every tag that a note has will be a unique string.  _Verify: existing tests still pass. A new test confrims that a newly created note has tags == []_
<paste the full brief you'll give Claude>
Add a list called tags to the app.notes' dict structure. Every newly created note should have an empty list assigned to tags. 
Then, ensure existing tests still pass and write a new test that ensures that upon creation of a new note, an empty list assigned to tags the app.notes dict exists.
Expected work should be in app.py in the /notes/new route so that you can add the tags element in the dict upon note creation. 
All existing tests should still pass. DO NOT EDIT THESE!!! The new tags feature should not affect their functionality- if they do, something's wrong.
Create a new test in tests/test_tags_feature.py that sets up the pytest file in the same way as the other pytest files and creates a new test that creates a new note and then asserts that the tags element in the app.notes dict is equal to an empty list. Plan everything out first, don't make any changes yet.

## Task 1 — what happened
- Claude's plan summary:
Here is the complete plan — no code written yet.

---
Plan

1. app.py — one-line change in /notes/new

Line 33, change:
app.notes.append({"title": title, "body": body})
to:
app.notes.append({"title": title, "body": body, "tags": []})

That's the only change needed. The tags key is added at note-creation time with an empty list. No other logic touches this path.

Why existing tests stay green:
- test_empty_title_shows_error / test_empty_body_shows_error — both return early before append, so they're unaffected.
- test_valid_submit_redirects_and_persists — checks n["title"] == "First", which still holds.
- test_invalid_submit_preserves_typed_values — never reaches append.

---
2. tests/test_tags_feature.py — new test file

Set it up the same way as test_task_01.py: no local fixture definitions (those live in conftest.py and are automatically picked up by pytest), just a module docstring and one test function.

"""Tests for the tags feature — every new note must start with an empty tags list."""


def test_new_note_has_empty_tags(client, app):
    app.notes.clear()
    client.post("/notes/new", data={"title": "Tagged Note", "body": "Some content"})
    assert app.notes[0]["tags"] == []

What this does:
1. Clears app.notes so there's no leftover state from other tests.
2. POSTs a valid new note via the test client.
3. Directly inspects app.notes[0]["tags"] and asserts it equals [].

---
- What I pushed back on: Wanted claude to check status code to make sure it redirected and no error was thrown on the website side that way the test didn't have a false positive if the dict updated but the page crashed as a result
- Verification result: Used pytest to assert that a tags field was added to newly created notes and manually added new note to ensure website didn't crash upon note creation

## Task 2 — what happened
- ...

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)